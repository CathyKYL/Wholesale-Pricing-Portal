name: Daily Keepa Price Update

on:
  # Run daily at 3:00 AM UTC
  schedule:
    - cron: '0 3 * * *'
  
  # Allow manual trigger from GitHub Actions tab
  workflow_dispatch:

jobs:
  update-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Verify DATABASE_URL is set
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          if [ -z "$DATABASE_URL" ]; then
            echo "❌ ERROR: DATABASE_URL secret is not set!"
            echo "Go to: https://github.com/${{ github.repository }}/settings/secrets/actions"
            exit 1
          else
            echo "✅ DATABASE_URL is set"
          fi
      
      - name: Verify KEEPA_API_KEY is set
        env:
          KEEPA_API_KEY: ${{ secrets.KEEPA_API_KEY }}
        run: |
          if [ -z "$KEEPA_API_KEY" ]; then
            echo "❌ ERROR: KEEPA_API_KEY secret is not set!"
            echo "Go to: https://github.com/${{ github.repository }}/settings/secrets/actions"
            exit 1
          else
            echo "✅ KEEPA_API_KEY is set"
          fi
      
      - name: Run daily Keepa update
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          KEEPA_API_KEY: ${{ secrets.KEEPA_API_KEY }}
        run: |
          cd backend/app
          python keepa_ingestor.py --daily
      
      - name: Verify update completed
        if: success()
        run: |
          echo "✅ Daily Keepa update completed successfully!"
          echo "Date: $(date)"
          echo "Records updated: Check logs above"
      
      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Daily update failed!"
          echo "Check the logs above for error details"
          exit 1





